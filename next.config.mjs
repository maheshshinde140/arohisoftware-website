/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'www.xoriant.com',
      'dynamicmedia.accenture.com',
      'img.freepik.com'
    ],
  },
};

export default nextConfig;
